import React from "react";
import StaticPage from "../components/StaticPage";

function CookiesPage() {
  return (
    <StaticPage title="Cookie policy" intro="Draft placeholder.">
      <p>
        This prototype uses essential local storage for cart and session demo. A
        live site would document analytics and marketing cookies with consent
        tools.
      </p>
    </StaticPage>
  );
}

export default CookiesPage;
