/**
 * Re-export — use `CategoryGrid` from `./CategoryGrid.jsx` for new code.
 * Keeps legacy `import … from "…/categories"` working on case-insensitive disks.
 */
export { default, CATEGORY_CARDS } from "./CategoryGrid";
